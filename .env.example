# GitHub Personal Access Tokens
# PAT_READ: Token for reading repositories (needs repo:read or public_repo scope)
PAT_READ=your_read_token_here

# PAT_WRITE: Token for writing to the README repository (needs repo scope for that specific repo)  
PAT_WRITE=your_write_token_here

# Optional: Additional user emails to track (comma-separated)
# USER_EMAILS=user@example.com,another@email.com

# Optional: Override default settings
# RUST_LOG=debug

# Note: username and repo_name are now required CLI arguments
# Usage: github-stats-updater --username YOUR_USERNAME --repo-name YOUR_REPO_NAME