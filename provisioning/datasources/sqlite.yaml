apiVersion: 1

datasources:
  - name: GitHub Stats SQLite
    uid: github-stats-sqlite
    type: frser-sqlite-datasource
    url: /tmp/github_stats.db
    isDefault: true
    editable: true
    jsonData:
      path: /tmp/github_stats.db